name: Continuous Integration

on:
  push:
    branches: [ main, master, dev ]
  pull_request:
    branches: [ main, master ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  java-matrix-test:
    name: Java Tests (JDK ${{ matrix.java-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java-version: [ 8, 17, 21 ]
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java-version }}

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Run Java Unit Tests across Core and Client
        run: ./gradlew test --no-daemon

      - name: Build All Jars
        run: ./gradlew build -x test --no-daemon

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-reports-jdk-${{ matrix.java-version }}
          path: |
            core/build/reports/tests/test/
            client/build/reports/tests/test/
          retention-days: 7

  launcher-lint-and-typecheck:
    name: Launcher Frontend Lint & Typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'launcher/package.json'

      - name: Install Frontend Dependencies
        working-directory: launcher
        run: npm ci

      - name: Run TypeScript Strict Typecheck
        working-directory: launcher
        run: npm run build

  rust-tauri-check:
    name: Rust Tauri Core Lint & Test
    runs-on: windows-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Setup Cargo Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            launcher/src-tauri/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('launcher/src-tauri/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Run Cargo Check & Clippy
        working-directory: launcher/src-tauri
        run: |
          cargo check --verbose
          cargo clippy -- -D warnings

      - name: Run Rust Unit Tests
        working-directory: launcher/src-tauri
        run: cargo test --verbose
